/** @type {import('next').NextConfig} */
const nextConfig = {
 /*  i18n: {
    locales: ["en-US", "ba"],
    defaultLocale: "en-US",
    localeDetection: false,
  },*/
}; 

export default nextConfig;
